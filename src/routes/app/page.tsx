import { AppCommand, AppCommandDialog } from "@/command";

export function IndexPage() {
  return (
    <section className="flex justify-center pt-[10rem]">
      <AppCommandDialog />
    </section>
  );
}
